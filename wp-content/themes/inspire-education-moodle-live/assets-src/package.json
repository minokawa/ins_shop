{
  "name": "inspire-asset-compile",
  "version": "1.0.0",
  "description": "Compilation config for the theme.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-dev": "sass --watch --update --style=expanded scss:../assets/css",
    "sass-prod": "sass --no-source-map --style=compressed scss:../assets/css"
  },
  "author": "inspire-marketing-dev",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.50.0"
  }
}
